def sphere(time, angle, density, radius, projection=lambda pos: [pos[0], pos[1]]):
    """
    Returns point on sphere spinning around sphere
    Default projection is orthographic
    """
    
    return